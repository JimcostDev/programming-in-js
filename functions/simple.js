// Declaración de la función
function sumar(a, b) {
    return a + b;  // Retorna la suma de los dos parámetros
  }
  
  // Llamada a la función
  let resultado = sumar(5, 3); // Pasa los argumentos 5 y 3 a la función
  console.log(resultado);  // Salida: 8
  